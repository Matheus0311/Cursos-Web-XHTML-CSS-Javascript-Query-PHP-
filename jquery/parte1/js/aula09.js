$(document).ready(function(){
	$("span").click(function(){
		
		// alert($("p").html());
		 
		// $("p").html("Mata a alma e a envenena");
		
		// $("p").html("<h1>Mata a alma e a envenena</h1>");
		
		// $("div #nada").text("<h1>Mata a alma e a envenena</h1>");
		// $("div #nada").html("<h1>Mata a alma e a envenena</h1>");
		
		// $("div #nada").text($("p").html());

// --------------------=>	val()	<=--------------------

		// alert($("textarea").val()); //apresenta o que está escrito 
		
		// $("p").html($("textarea").val()); // apresenta o que foi escrito na textarea dentro da tag p 
		
		// $("p").text($("textarea").val()); // mais seguro pois não injeta código 
		
// --------------------=>	replaceWith()	<=--------------------
		
		//remove e substitui tudo que for colocado
		
		// troca a div por um texto
		// $("div #nada").replaceWith($("textarea").val()); 
		
		
		
	});
});